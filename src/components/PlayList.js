import React from 'react';

const PlayList = () => {
  return (
      <React.Fragment>
        {(
            <div className="playlist">
              Hello playlist
            </div>
        )}
      </React.Fragment>
  );
};
export default PlayList;